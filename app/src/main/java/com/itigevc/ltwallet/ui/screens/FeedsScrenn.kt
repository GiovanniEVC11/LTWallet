package com.itigevc.ltwallet.ui.screens
/*
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.material.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.navigation.NavController
import com.itigevc.ltwallet.ui.components.BottomNavItem
import com.itigevc.ltwallet.ui.components.BottomNavMenu

@Composable
fun FeedsScreen(
    navController: NavController
){

    Column(modifier = Modifier.fillMaxSize()){

        Column(modifier = Modifier.weight(1f)){
            Text(text = "Feeds Screen")
        }

        BottomNavMenu(
            selectedItem = BottomNavItem.PROFILE,
            navController = navController
        )

    }

}

 */